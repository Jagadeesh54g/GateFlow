# Copy to .env.local for local dev.
# In production, set these as Environment Variables in the Vercel project dashboard instead.

# Your MongoDB Atlas connection string (Atlas → Connect → Drivers).
# Include your db user's password, and make sure your IP (or 0.0.0.0/0) is allowlisted.
MONGODB_URI=mongodb://jagadeeshgarnimitta9:dsHoPA865oPIi3Lq@ac-7venmmu-shard-00-00.xxppndt.mongodb.net:27017,ac-7venmmu-shard-00-01.xxppndt.mongodb.net:27017,ac-7venmmu-shard-00-02.xxppndt.mongodb.net:27017/?ssl=true&replicaSet=atlas-mbkxkv-shard-0&authSource=admin&appName=test
MONGODB_DB=gateflow
# Vercel Blob storage token (Storage tab in Vercel dashboard → create a Blob store →
# it auto-generates this and injects it into your deployed project's env vars).
# For local dev: Storage → your Blob store → .env.local tab → copy the token here.
BLOB_STORE_ID="store_ZUrf0kNjcOVOwDgw"
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_ZUrf0kNjcOVOwDgw_oV8xFSIeosPYAs06Ry0OXJRgFpKTet"
